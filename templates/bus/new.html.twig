{% extends 'base.html.twig' %}

{% block title %}Ajouter un Bus - Kar&Go{% endblock %}

{% block body %}
<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card shadow-sm">
                <div class="card-header bg-primary text-white">
                    <h2 class="mb-0">
                        <i class="bi bi-plus-circle me-2"></i>
                        Ajouter un Nouveau Bus
                    </h2>
                </div>
                <div class="card-body">
                    {{ form_start(form) }}
                    
                    <div class="mb-3">
                        {{ form_label(form.numero, null, {'label_attr': {'class': 'form-label fw-bold'}}) }}
                        {{ form_widget(form.numero) }}
                        {{ form_errors(form.numero) }}
                        <small class="form-text text-muted">
                            <i class="bi bi-info-circle me-1"></i>
                            Format recommandé : TUN-XXXX-XX
                        </small>
                    </div>

                    <div class="mb-3">
                        {{ form_label(form.capacite, null, {'label_attr': {'class': 'form-label fw-bold'}}) }}
                        {{ form_widget(form.capacite) }}
                        {{ form_errors(form.capacite) }}
                        <small class="form-text text-muted">
                            <i class="bi bi-info-circle me-1"></i>
                            Nombre de places disponibles (entre 10 et 100)
                        </small>
                    </div>

                    <div class="mb-3">
                        {{ form_label(form.type, null, {'label_attr': {'class': 'form-label fw-bold'}}) }}
                        {{ form_widget(form.type) }}
                        {{ form_errors(form.type) }}
                    </div>

                    <div class="d-flex justify-content-between mt-4">
                        <a href="{{ path('app_admin_bus_index') }}" class="btn btn-secondary">
                            <i class="bi bi-arrow-left me-2"></i>Retour
                        </a>
                        {{ form_widget(form.submit) }}
                    </div>

                    {{ form_end(form) }}
                </div>
            </div>

            <!-- Aide contextuelle -->
            <div class="card mt-3 bg-light">
                <div class="card-body">
                    <h5 class="card-title">
                        <i class="bi bi-lightbulb text-warning me-2"></i>
                        Conseils pour ajouter un bus
                    </h5>
                    <ul class="mb-0">
                        <li>Assurez-vous que le numéro de matricule est unique</li>
                        <li>La capacité déterminera le nombre de places disponibles pour les réservations</li>
                        <li>Le type de bus affecte le confort et le prix du trajet</li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
    .form-control:focus {
        border-color: #7c3aed;
        box-shadow: 0 0 0 0.2rem rgba(124, 58, 237, 0.25);
    }
    
    .form-label.fw-bold {
        color: #374151;
    }
</style>
{% endblock %}